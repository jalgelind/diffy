.vscode
.python-version
# All build trees live under out/ (Makefile, build-windows.cmd, CI). A stray
# top-level build*/ dir is intentionally NOT ignored so it surfaces as untracked.
out/
tests/_test_runner
.ccls-cache/
scratch/
